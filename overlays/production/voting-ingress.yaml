apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jpo-ingress
  annotations:

    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: 10.9.2.112
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vote
            port:
              number: 8080